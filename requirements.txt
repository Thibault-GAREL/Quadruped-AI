# Core simulation
pygame>=2.6.0
box2d-py>=2.3.5

# IA neuroevolution + tracking
numpy>=1.26.0
pandas>=2.0.0
mlflow>=3.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Entrainement headless (train.py)
tqdm>=4.66.0

# PPO (train.py --algo ppo). Installer torch selon la machine :
#   Runpod CPU : pip install torch --index-url https://download.pytorch.org/whl/cpu
#   Local GPU (CUDA 12.1) : pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0
